#include <stdio.h>
/**
 * main - program to print all numbers zero to ten
 *
 * Return: Always (0) positive
 */
int main(void)
{
	int i;
	
	for (i = 1; i < 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
